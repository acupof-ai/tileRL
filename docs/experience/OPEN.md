# Open defects

Documented in `errors/` with a fix named and not landed. A PR that lands the fix removes
its line. Writing the entry and adding the line are one act — an entry with `Status: open`
and no line here is the same defect this file exists to stop. Reviewers check a change on the same path against this list.

| entry | path | fix |
|---|---|---|
| [the rollouts grew into the cap](errors/2026-09-06-the-rollouts-grew-into-the-cap.md) | `src/tilerl/eval.py` `MATCHERS`, GRPO advantage | a length term in the reward or a length-aware advantage — `boxed_match` is correctness-only, so nothing prefers the shorter of two correct answers and the policy lengthens until it truncates |
| [a spec rate over a dense roofline](errors/2026-09-06-a-spec-rate-over-a-dense-roofline.md) | a draft step's streamed bytes, nowhere bucketed | extend `scripts/check_scale_f16.py`'s role buckets to the draft path so the speculative ceiling is a number instead of the 32-42% bracket — the four wrong denominators are fixed, but the bracket's width is one unmeasured operand, and 0.85 GB is the head's *resident* size, not what a draft forward streams |
| [a number with no instrument](errors/2026-09-06-a-number-with-no-instrument.md) | five citations of the "~60 µs eager launch floor" | run `scripts/probe_launch_floor.py` on an idle V100 and replace the figure with the measurement, or delete the claim from all five sites — the probe exists and refuses a busy card, and the endpoint holds 28.0 of 32.8 GB |
| [the NCCL floor has no instrument](errors/2026-09-06-the-nccl-floor-has-no-instrument.md) | ten citations of "21.5 µs NCCL floor" across `docs/design-parallel.md`, `docs/roadmap.md`, `CHANGELOG.md:357` | run `torchrun --nproc_per_node=8 scripts/nccl_probe.py` on the H20 and replace the figure, or mark all ten sites unmeasured — the probe exists and nothing cites it, and the ring-vs-all-gather verdict at `design-parallel.md:191` rests on the number |
| [the save stage had no timer](errors/2026-09-06-the-save-stage-had-no-timer.md) | `max_pending=32` at `src/tilerl/kv_cache.py:405`, and the pod's write rate | run `scripts/probe_save_ms.py` on the pod's `/work` from inside the container namespace, then size `max_pending` against the measured per-save cost — the five `~100 ms` comments are corrected to the 641.8 ms measured here, but the cap still holds 32 × 320.6 MiB = 10.0 GiB of a 31 GB host and was set against a figure 6.4x low |
| [the OOM was micro=0](errors/2026-09-06-the-oom-was-micro-zero.md) | `--micro`'s help in `src/tilerl/cli.py` | say at the point of use that `--micro 0` at group 8 / cap 2048 peaks 88.21 GiB and OOMs step 2 on one H20, against 44.55 GiB at `--micro 1` — the recipes already set 1, so only a hand-typed run walks into it, and nothing warns |
| [the OOM was micro=0](errors/2026-09-06-the-oom-was-micro-zero.md) | `scripts/pod_sync.sh:28` | exempt `runs/` from the `find . -mindepth 1 -delete` the way line 12 already rescues `bench-baseline.json` — every peer's run manifests die on the next sync by anyone, and run 2's peak_gib was destroyed this way while being needed |
| [stop sequences accepted and never applied](errors/2026-09-06-stop-sequences-accepted-and-never-applied.md) | `messages.py` `stop_sequences`, and `stop` on chat completions / Responses where it is not even a declared field | plumb the strings to a new `SamplingParams.stop_ids` matched in the `engine.py:1129` branch that already honours `stop_token_ids`, and truncate `req.output` to the match before finishing — a core field of both APIs, refused with 400 today; single-token stops already work through `stop_token_ids`, the multi-token case is why this is not a one-liner, record-vs-reply contract settled 2026-09-06 (record keeps the sampled ids through the completing token, reply cut at the match start, JSONL row carries the reason), so what is left is `k` for the rolling decode |
