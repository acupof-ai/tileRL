"""Operator layer: the only tilerl modules that talk to tilelang directly.
backend (dispatch), registry (precision x arch matrix), kernels* (TileLang),
reference (torch-eager parity oracle and day-1 backward)."""
